# Python
__pycache__/
*.pyc

# Per-machine GitHub account marker (multi-account routing)
.gh-account

# Per-machine Claude Code project config (project-local commands, agents, permissions)
.claude/

# Maintainer-only Claude instructions (loaded alongside CLAUDE.md, not shared)
CLAUDE.local.md

# Craft workspace - maintainer's local state, not distributed
.craft/

# Local research scratch - /craft:research output, not distributed
research/

# (Previous fine-grained patterns below are now redundant under the blanket .craft/ rule.
# Kept commented out for reference; remove during a follow-up cleanup if desired.)
# .craft/.global-state
# .craft/.continuation
# .craft/.chunk-state
# .craft/.active-fix
# .craft/.pinned-project
# .craft/.exports/
# .craft/cycles/*/.state
# .craft/cycles/.state
# .craft/cycles/*/.events/
# .craft/cycles/*/.usage/
# .craft/cycles/*/.failures
# .craft/cycles/*/.craft/
# .craft/browser/snapshots/
